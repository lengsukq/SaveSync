# SaveSync 实现总结

## 已完成功能

### 1. 多游戏支持 ✅
- 支持添加多个游戏存档配置
- 每个游戏存档独立管理
- 支持自定义游戏名称、路径、描述

### 2. 游戏存档配置 ✅
- **基本信息**：
  - 游戏名称
  - 存档路径（通过文件选择器选择）
  - 描述信息（可选）
  - 启用/禁用状态

- **WebDAV 配置**（可选）：
  - WebDAV URL
  - 用户名
  - 密码
  - 配置后备份自动上传到云端

### 3. 本地备份功能 ✅
- 使用 **ZIP 格式**压缩存档
- 备份文件存储在本地备份目录
- 自动记录备份时间、大小等信息
- 支持查看备份历史

### 4. WebDAV 云端备份 ✅
- 配置 WebDAV 后，备份自动上传到云端
- 使用 ZIP 格式上传
- 云端备份标记为 "cloud" 类型
- 支持从云端下载备份

### 5. 备份恢复功能 ✅
- **本地备份恢复**：
  1. 读取本地 ZIP 文件
  2. 解压到临时目录
  3. 覆盖到游戏存档目录

- **云端备份恢复**：
  1. 从 WebDAV 下载 ZIP 文件到临时目录
  2. 解压到临时目录
  3. 覆盖到游戏存档目录

- 恢复前自动备份当前存档（安全措施）

### 6. 备份管理 ✅
- 查看所有备份历史
- 按游戏筛选备份
- 删除本地或云端备份
- 显示备份类型（本地/云端）
- 显示备份大小和创建时间

## 技术实现

### 后端（Rust）
- **ZIP 压缩/解压**：使用 `zip` crate
- **WebDAV 客户端**：使用 `reqwest` crate（blocking 模式）
- **文件操作**：使用标准库 `std::fs`
- **数据持久化**：JSON 文件存储

### 前端（React + TypeScript）
- **UI 框架**：HeroUI
- **状态管理**：Zustand
- **类型安全**：完整的 TypeScript 类型定义
- **表单验证**：前端验证 + 后端验证

## 数据模型

### GameSave
```typescript
{
  id: string;
  name: string;
  savePath: string;
  description?: string;
  enabled: boolean;
  lastBackup?: Date;
  lastSync?: Date;
  backupCount: number;
  webdavUrl?: string;
  webdavUsername?: string;
  webdavPassword?: string;
}
```

### Backup
```typescript
{
  id: string;
  gameSaveId: string;
  name: string;
  path: string;  // ZIP 文件路径
  size: number;
  createdAt: Date;
  type: 'local' | 'cloud';
}
```

## 文件结构

### 备份存储
- **本地备份**：`{数据目录}/backups/{游戏名}_{时间戳}.zip`
- **云端备份**：`{WebDAV URL}/{游戏ID}/{游戏名}_{时间戳}.zip`
- **临时文件**：`{数据目录}/temp/`（恢复时使用）

### 数据文件
- **游戏存档配置**：`{数据目录}/game_saves.json`
- **备份记录**：`{数据目录}/backups.json`

## 工作流程

### 创建备份
1. 用户点击"立即备份"
2. 系统检查存档路径是否存在
3. 将存档目录压缩为 ZIP 文件
4. 保存到本地备份目录
5. 如果配置了 WebDAV，上传到云端
6. 更新备份记录和游戏存档信息

### 恢复备份
1. 用户选择要恢复的备份
2. 如果是云端备份，先从 WebDAV 下载
3. 解压 ZIP 文件到临时目录
4. 备份当前存档（如果存在）
5. 删除现有存档目录
6. 将解压的文件复制到存档目录
7. 清理临时文件

## 安全特性

1. **恢复前备份**：恢复前自动备份当前存档
2. **错误处理**：完善的错误处理和用户提示
3. **路径验证**：验证存档路径是否存在
4. **临时文件清理**：自动清理临时文件

## 待优化项

1. 添加备份进度显示
2. 支持批量备份/恢复
3. 添加备份自动清理（保留最近 N 个）
4. 支持备份加密
5. 添加备份验证（校验和）
